# Bundled fonts

Latin woff2 files are vendored for offline use. Chinese glyphs fall back to Windows system UI fonts (`Microsoft YaHei UI`).

Each `licenses/` file is the upstream license for that family (SIL Open Font License unless noted by the file itself).
